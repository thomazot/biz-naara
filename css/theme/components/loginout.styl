.loginout
    color: var(--tertiary)
    font-weight: regular
    font-size: 12px

    .header &
        margin-left: 50px

        @media (max-width: 992px)
            display: none

    a
        color: var(--tertiary)

        &:hover
            color: var(--secundary)